@model IEnumerable<E_Trade.MvsWebUI.Entity.Category>
<ul class="list-group">
    <li class="list-group-item active" id="group-title">Cras justo odio</li>
    @foreach (var item in Model)
    {
        <li class="list-group-item">
        <a href="/Home/List/@item.Id"></a>
    @item.Name</li>
    }
</ul>
   
